# AGENTS.md

## 1. 프로젝트 목표

서울 지역에서 사용자가 출발지와 도착지를 입력하고 다음 지표 중 하나를 선택하면, 일반 최단 경로와 지표 최적화 경로를 함께 보여주는 Streamlit 기반 지도 길찾기 MVP를 구현한다.

지원 모드:

- 여름 모드: 그늘이 많은 보행 경로
- 가을 모드: 은행나무 암나무 밀집 구간을 피하는 경로
- 겨울 모드: 결빙 위험을 줄이고 도로 열선 구간을 선호하는 경로
- 안심 모드: 가로등이 많고 야간 보행에 유리한 경로

추가 기능:

- 도보 경로가 1.2km 또는 약 15분 이상이면 따릉이 복합 이동 가능성을 안내한다.
- 일반 경로와 맞춤 경로의 거리, 예상 시간, 우회 거리, 지표 점수를 비교한다.
- 이동 완료 시 택시비 절감액과 탄소 절감량을 보여준다.
- 로그인 없이 로컬 세션 및 로컬 파일 기반 월간 이용 리포트를 제공한다.

## 2. MVP 원칙

- 먼저 API 키 없이 로컬에서 실행 가능한 데모를 완성한다.
- 실제 외부 API나 공공데이터가 없으면 서울 지역 샘플 데이터를 사용한다.
- 샘플 데이터 사용 여부를 코드와 화면에 명확히 표시한다.
- 존재하지 않는 API 응답이나 실제 학습 성능을 꾸며내지 않는다.
- 로그인, 결제, 관리자 페이지, 소셜 기능은 구현하지 않는다.
- 핵심 사용자 흐름을 완성하기 전에는 배포와 고급 최적화를 진행하지 않는다.
- 작업 중 합리적인 기본값을 선택하고 계속 진행한다. API 키나 데이터 파일처럼 개발자가 반드시 제공해야 하는 값만 마지막에 목록으로 요청한다.

## 3. 핵심 사용자 흐름

1. 사용자가 서울 내 출발지와 도착지를 입력한다.
2. 사용자가 여름, 가을, 겨울, 안심 모드 중 하나를 선택한다.
3. 앱이 입력 주소를 좌표로 변환한다.
4. 앱이 일반 최단 경로를 계산한다.
5. 앱이 선택한 모드의 공간 지표와 강화학습 정책을 이용해 맞춤 경로를 계산한다.
6. 지도에 일반 경로와 맞춤 경로를 서로 다른 색상으로 동시에 표시한다.
7. 두 경로의 거리, 시간, 우회율, 지표 점수와 주요 근거를 표시한다.
8. 도보 거리가 기준을 넘으면 따릉이 복합 경로 안내를 표시한다.
9. 이동 완료 버튼을 누르면 비용 및 탄소 절감 결과를 표시하고 월간 통계에 반영한다.

## 4. 권장 기술 스택

- Python 3.11 이상
- Streamlit
- streamlit-folium 또는 Folium
- GeoPandas
- Shapely
- OSMnx
- NetworkX
- Pandas, NumPy
- scikit-learn
- joblib
- pytest
- Ruff
- 필요 시 python-dotenv

가능하면 무료이고 API 키가 필요 없는 OpenStreetMap 기반 구현을 기본으로 사용한다. 네이버, 구글, 티맵 API는 adapter 인터페이스 뒤에 선택적으로 연결한다.

## 5. 권장 디렉터리 구조

```text
.
├── AGENTS.md
├── README.md
├── app.py
├── requirements.txt
├── .env.example
├── config/
│   └── settings.py
├── data/
│   ├── sample/
│   ├── processed/
│   └── README.md
├── models/
│   └── README.md
├── scripts/
│   ├── prepare_data.py
│   └── train_rl.py
├── src/
│   ├── geocoding.py
│   ├── map_view.py
│   ├── routing/
│   │   ├── graph.py
│   │   ├── baseline.py
│   │   ├── weighted.py
│   │   ├── rl_agent.py
│   │   └── service.py
│   ├── indicators/
│   │   ├── base.py
│   │   ├── shade.py
│   │   ├── ginkgo.py
│   │   ├── heating.py
│   │   └── safety.py
│   ├── mobility/
│   │   └── bike.py
│   ├── reporting/
│   │   ├── carbon.py
│   │   ├── briefing.py
│   │   └── monthly.py
│   └── data_sources/
│       ├── base.py
│       ├── sample.py
│       └── seoul_api.py
└── tests/
```

구조는 필요에 따라 단순화할 수 있지만 UI, 경로 탐색, 지표 계산, 데이터 접근, 리포팅 로직을 한 파일에 모두 넣지 않는다.

## 6. 지도 및 경로 규칙

- 보행 네트워크는 OSMnx의 walking network를 기본으로 사용한다.
- 일반 경로는 거리 또는 예상 보행 시간을 비용으로 사용하는 Dijkstra/A*로 계산한다.
- 맞춤 경로는 다음 형태의 edge cost를 기본으로 한다.

```text
adjusted_cost =
    normalized_distance
    + detour_weight
    + mode_risk_weight
    - mode_benefit_weight
```

- 모든 지표는 가능한 한 0~1 범위로 정규화한다.
- 음수 edge cost가 발생하지 않도록 최솟값을 제한한다.
- 맞춤 경로의 최대 우회율은 기본 25%로 제한한다.
- 조건을 만족하는 맞춤 경로가 없으면 일반 경로를 반환하고 이유를 사용자에게 알린다.
- 경로 결과에는 사용된 지표 점수와 계산 근거를 포함한다.
- 좌표계와 거리 단위를 명시하고, 공간 연산 전 좌표계를 검증한다.

모드별 기본 의미:

- shade_score가 높을수록 여름 모드에서 유리하다.
- ginkgo_risk가 높을수록 가을 모드에서 불리하다.
- heating_score가 높을수록 겨울 모드에서 유리하다.
- icing_risk가 높을수록 겨울 모드에서 불리하다.
- light_score와 safety_score가 높을수록 안심 모드에서 유리하다.

## 7. 강화학습 요구사항

강화학습 기능은 실제 학습, 모델 저장, 모델 로딩, 추론 연결이 모두 존재해야 한다.

MVP에서는 다음 방식을 기본으로 한다.

- 출발지와 도착지 주변의 제한된 보행 부분 그래프를 환경으로 사용한다.
- 상태는 현재 노드, 목적지와의 거리 구간, 선택 모드의 지표 구간으로 구성한다.
- 행동은 현재 노드에서 이동 가능한 인접 edge를 선택하는 것이다.
- 보상은 목적지 도착 보상, 거리 비용, 반복 방문 패널티, 최대 우회 패널티, 모드별 쾌적성 보상으로 구성한다.
- 재현 가능한 random seed를 사용한다.
- tabular Q-learning 또는 이에 준하는 가벼운 정책 학습을 우선 구현한다.
- 학습 결과와 메타데이터를 models/에 저장한다.
- 추론 실패, 미학습 상태, 허용 우회율 초과 시 weighted A*로 안전하게 fallback한다.
- UI에서 현재 결과가 RL 정책, weighted A*, 또는 일반 경로 중 무엇으로 생성됐는지 표시한다.
- 학습 전후 평균 보상, 목적지 도달률, 평균 우회율을 평가한다.
- 학습 효과가 검증되지 않았다면 “강화학습이 더 좋은 경로를 만들었다”고 표현하지 않는다.

강화학습은 경로 안전성을 보장하는 수단으로 표현하지 않는다. 안심 및 결빙 경로는 참고용이며 공식 안전 경로가 아니라는 짧은 안내를 화면에 표시한다.

## 8. 데이터 처리 규칙

각 데이터셋은 다음 메타데이터를 data/README.md에 기록한다.

- 데이터셋 이름
- 제공 기관
- 원본 URL
- 내려받은 날짜 또는 API 조회 시각
- 라이선스
- 좌표계
- 주요 컬럼
- 전처리 방법
- 샘플 데이터 여부

실제 데이터가 준비되지 않은 경우:

- 서울 중심부의 작은 시연 영역에 대한 합성 GeoJSON/CSV를 만든다.
- 합성 데이터에는 `is_sample=true` 필드를 둔다.
- 실제 지명이나 시설 위치라고 오해할 수 있는 정밀한 주장을 하지 않는다.
- adapter 인터페이스는 실제 데이터 소스로 교체 가능하게 설계한다.

은행나무 데이터가 암수 정보를 제공하지 않으면 임의로 암나무를 판정하지 않는다. 해당 컬럼이 없는 데이터는 “은행나무 전체 회피”로 처리하거나 기능 제한을 표시한다.

시간별 그늘 데이터가 없으면 건물·가로수·시간을 이용한 정밀 그림자 계산을 주장하지 않는다. MVP에서는 수관 또는 그늘 시설 주변 buffer를 이용한 shade proxy임을 명시한다.

## 9. 외부 API와 비밀정보

- API 키와 비밀정보를 저장소에 커밋하지 않는다.
- `.env.example`에는 변수 이름과 설명만 기록한다.
- 애플리케이션은 API 키가 없어도 샘플 모드로 실행되어야 한다.
- 외부 호출에는 timeout, 오류 처리, 간단한 캐시를 둔다.
- API별 adapter를 사용하여 UI와 공급자 SDK가 직접 결합되지 않게 한다.
- 유료 API나 새 production dependency를 추가하기 전에는 필요성과 무료 대안을 설명한다.
- API 약관상 지도 타일, 경로, 검색 결과를 서로 결합할 수 있는지 확인한다.

예상 환경 변수:

```text
SEOUL_OPEN_API_KEY=
VWORLD_API_KEY=
KAKAO_REST_API_KEY=
TMAP_APP_KEY=
OPENAI_API_KEY=
```

실제로 사용하지 않는 키는 요구하지 않는다.

## 10. 기후 브리핑과 탄소 계산

- 기본 브리핑은 템플릿 기반으로 생성하여 OpenAI API 키 없이도 작동하게 한다.
- OpenAI API 연결은 선택 기능으로 구현한다.
- LLM에는 계산된 구조화 통계만 전달하고, 거리나 탄소량 계산을 LLM에 맡기지 않는다.
- 비용과 탄소 계산식 및 가정값을 코드 상수와 화면 도움말에 표시한다.
- “택시비를 절감했다”는 사용자가 택시 대신 걸었다고 확인한 경우에만 사용한다.
- 월간 리포트는 로그인 없이 로컬 JSON 또는 SQLite에 저장한다.
- 개인정보와 정밀 이동 이력은 MVP에서 서버로 전송하지 않는다.

## 11. UI 요구사항

- 모든 사용자 문구는 자연스러운 한국어로 표시한다.
- 첫 화면에서 출발지, 도착지, 모드, 검색 버튼이 바로 보여야 한다.
- 일반 경로는 회색 또는 파란색, 맞춤 경로는 모드별 강조색으로 표시한다.
- 지표 위치 또는 밀도를 지도 layer로 표시하고 켜고 끌 수 있게 한다.
- 지도 아래에 두 경로 비교 카드 또는 표를 표시한다.
- 로딩, 주소 검색 실패, 서울 외 지역, 경로 없음, API 오류 상태를 처리한다.
- 모바일 너비에서도 핵심 입력과 결과가 사용할 수 있어야 한다.
- 데모 데이터 사용 중이면 화면 상단에 “샘플 데이터 기반 데모” 배지를 표시한다.

## 12. 테스트와 품질 기준

변경 범위에 맞는 테스트를 실행한다.

필수 테스트 대상:

- edge 가중치 계산
- 지표 정규화
- 최대 우회율 제한
- 일반 경로와 맞춤 경로 생성
- RL 모델 저장 및 로딩
- RL 추론 실패 시 fallback
- 탄소 및 비용 계산
- 샘플 데이터 로딩

완료 전 다음을 확인한다.

```text
pytest
ruff check .
streamlit run app.py
```

Streamlit 앱은 직접 실행하여 시작 오류가 없는지 확인한다. 가능하면 대표 출발지와 도착지로 사용자 흐름을 점검한다.

## 13. 작업 방식

- 작업을 시작할 때 현재 저장소 상태와 기존 파일을 먼저 확인한다.
- 기존 사용자 변경사항을 보존한다.
- 큰 작업은 작은 단계로 나누되, 승인받은 단계의 구현과 검증까지 완료한다.
- 각 단계가 끝나면 변경된 파일, 실행 방법, 검증 결과, 남은 제한사항을 짧게 보고한다.
- 단순한 계획만 제시하고 멈추지 말고 요청된 구현까지 진행한다.
- 불명확한 세부사항은 MVP에 적합한 합리적 기본값을 선택하고 그 가정을 기록한다.
- 꼭 필요한 API 키, 데이터 파일, 약관 선택만 개발자에게 요청한다.

## 14. MVP 완료 기준

다음 조건이 모두 충족되면 MVP가 완료된 것으로 본다.

- `streamlit run app.py`로 앱이 실행된다.
- 서울 내 샘플 출발지와 도착지로 경로 검색이 가능하다.
- 일반 경로와 선택 모드의 맞춤 경로가 지도에 동시에 표시된다.
- 네 가지 모드가 모두 최소 샘플 데이터로 작동한다.
- 지표 layer를 지도에서 확인할 수 있다.
- 실제로 학습 및 저장된 RL 정책이 적어도 하나의 맞춤 경로 결정에 사용된다.
- RL 사용 여부와 fallback 여부가 결과에 표시된다.
- 1.2km 이상 경로에서 따릉이 안내가 나타난다.
- 완료 결과와 월간 누적 통계가 표시된다.
- 외부 API 키가 없어도 샘플 모드로 전체 흐름을 시연할 수 있다.
- README에 설치, 실행, 데이터, 알고리즘, 한계가 설명되어 있다.
- 핵심 테스트가 통과한다.